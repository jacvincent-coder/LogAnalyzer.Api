import { Routes } from '@angular/router';
import { LogAnalyzerComponent } from './components/log-analyzer/log-analyzer.component';

export const routes: Routes = [
  { path: '', component: LogAnalyzerComponent }
];
